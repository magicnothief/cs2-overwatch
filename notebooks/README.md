# notebooks

Exploration only. Name them `NN_topic.ipynb` (`01_first_demo`, `02_angles`, …).
Rule of thumb: once a piece of notebook code has been useful twice, move it into `src/` and
import it back.
