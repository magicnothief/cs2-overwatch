# data (not committed)

- `raw/`: `.dem` files and downloaded datasets, exactly as downloaded.
- `interim/`: normalized parquet from `parsing/`.
- `processed/`: feature tables, training windows, splits.

Keep `data/SOURCES.md` up to date: file → origin URL → date → license.
Demos contain SteamIDs and names, so never publish raw data.
